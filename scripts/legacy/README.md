# 历史脚本归档

来自原研究阶段的零散 shell 脚本笔记，保留供参考。

## 文件

- `environment_setup_notes.sh` — Linux/WSL 字体安装 + sed CRLF 转换的笔记
  （原文件为根目录 `code.sh`）

## 与 `scripts/` 主目录的区别

- `scripts/`（父目录）—— 原研究的完整 R / Python / shell 脚本，分类清晰
- `scripts/legacy/`（本目录）—— 没归类的临时笔记 / 一次性命令记录

## 整理时间

2026-05-07 从根目录 `code.sh` 迁移到 `scripts/legacy/environment_setup_notes.sh`。
